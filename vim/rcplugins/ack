" Ack - Search and populate quickfix window

Plug 'mileszs/ack.vim'

nmap <Leader>sc :Ack! <c-r>=expand('<cword>')<cr>
vnoremap <Leader>sc :<C-u>call <sid>AckVisualSelection()<CR>

function! s:AckVisualSelection()
  normal! gvy
  execute "Ack! '" . escape(@", "[]().*") . "'"
endfunction

let g:ackprg = 'ag --nogroup --nocolor --column'

" vim:ft=vim
